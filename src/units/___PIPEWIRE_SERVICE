___PIPEWIRE_SERVICE() {
    OBJ='/dev/null'
    OBJ_PATH="$home/.config/systemd/user/pipewire.service"
    __obj_link_ow_soft \
        -uid "$uid" -gid "$gid" \
        -user "$user" -group "$group"
}
